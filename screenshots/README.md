Coloca aquí tu captura de pantalla cuando la tengas:

- Ruta sugerida: `screenshots/screenshot.png`

Después de añadir la imagen, haz:

```bash
git add screenshots/screenshot.png
git commit -m "Añadir captura de pantalla"
git push origin main
```
